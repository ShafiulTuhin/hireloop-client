import Login from "@/components/authentication/Login";
import React from "react";

export const metadata = {
  title: "Hire loop | Login",
  description: "Login into website and find your job",
};

const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;
